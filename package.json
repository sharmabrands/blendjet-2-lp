{
  "name": "webflow-blendjet-2-lp",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "serve",
    "build": "node builder.js"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "serve": "^14.2.1"
  },
  "dependencies": {
    "vue": "^2.7.16"
  }
}
